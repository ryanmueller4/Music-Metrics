{
  "name": "music-metrics",
  "version": "1.0.0",
  "description": "An app that allows users to look up data about songs, albums, and artists.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"mongod\" \"node server/server.js\"",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bulma": "^1.0.0",
    "dotenv": "^16.4.5",
    "mongodb": "^6.5.0"
  }
}
